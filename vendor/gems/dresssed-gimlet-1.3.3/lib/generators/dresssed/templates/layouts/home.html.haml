- content_for :navbar do
  = form_tag "?", :class => "navbar-search pull-right" do
    = text_field_tag :query, params[:query], :class => "search-query"

= render :layout => "layouts/base" do
  = yield
